# UI_DESIGN.md

LINE Notion BotのUI設計ルールです。機能追加時に画面がごちゃごちゃしないことと、「押したのに反応がないように見える」状態を減らすことを重視します。

## 基本原則

- メインメニューに全機能を並べない。
- 1画面1目的を基本にする。
- 主要操作は3タップ以内。
- 通常操作はprimary、戻る・キャンセル・登録しないはsecondary。
- Notionに実データがある場合、Postbackへ値を詰め込みすぎずIDから再取得する。
- 危険操作・取り消しは対象を表示して確認画面を必ず挟む。
- コマンドを覚えていることを前提にせず、目的から到達できる入口を用意する。

---

# ボタンを押した瞬間の反応

Message actionは押したテキストがすぐトークに表示されます。Postbackには原則 `displayText` を付けます。

```text
ボタンを押す
↓
即時: ▶ 食費
↓
数秒後: Botの処理結果
```

---

# メインメニュー

```text
✨ 迷ったら
📊 家計簿・予算
🛒 特売・買い物
✏️ 修正・取り消し
💸 貸し借り
💳 カード
📝 メモ・あとで見る
🤖 AI検索・改善
🗂 Notion・その他
```

## 特売・買い物

```text
今日の特売を見る → 特売情報
買い物リストを見る → 買い物リスト
買い物を追加する → Phase4 の使い方
```

買い物リストは未完了項目だけ表示します。購入済み処理は `買った 商品名`。複数一致した場合は自動完了しません。

## メモ・あとで見る

```text
メモを追加する
メモ一覧を見る
Phase 4の使い方
メモを削除する
```

Phase 4の代表操作:

```text
メモ 住民票を明日までに提出
買い物 牛乳
買い物リスト
買った 牛乳
URLをそのまま送信
```

---

# Phase 4 UI

## #50 メモ期限 / #53 メモ自動分類

ユーザーは自然な文のまま送信します。

```text
メモ 住民票を明日までに提出
```

返答例:

```text
✅ メモを保存しました。
内容: 住民票をに提出
分類: やること
期限: 2026-09-15
```

期限の認識対象は `今日 / 明日 / 明後日 / YYYY-MM-DD / M/D`。分類は `買い物 / やること / 予定 / アイデア / その他`。

## #54 買い物リスト

```text
買い物 牛乳
↓
✅ メモを保存しました。
分類: 買い物

買い物リスト
↓
・牛乳
・洗濯ネット

買った 牛乳
↓
✅「牛乳」を購入済みにしました。
```

同じキーワードで複数候補がある場合は候補を表示し、勝手に完了しません。

## #56 URLタイトル取得 / #57 URLカテゴリ分類

URLを送った時点で保存処理へ進みます。

```text
https://example.com/article
↓
✅ URLを保存しました。
タイトル: ページタイトル
カテゴリ: 記事
ドメイン: example.com
```

カテゴリは `記事 / 買い物 / 動画 / SNS / 資料 / その他`。ページタイトル取得に失敗してもURL保存自体は止めません。

---

# Phase 3A UI

自然文家計簿は、金額・店名・日付を解析し、不足しているジャンル・支払方法を選択後、最終確認して保存します。

```text
今日サミットで2380円使った
支出テンプレート
本日のレポート
```

---

# Phase 3B UI

```text
直前登録
↓
金額 / 店名 / 日付 / ジャンル / 支払方法
↓
修正項目を選択
```

`直前取り消し` は確認必須で、Notionページをアーカイブします。

---

# ジャンル別予算

```text
予算 お菓子 3000
```

月別管理DBに `お菓子予算` がない場合は自動作成して保存します。

---

# カード未処理

- おすすめジャンルを表示。
- `固定費`は候補から除外。
- `サブスク`は表示。
- Postback選択時は `displayText` で即時反応。

---

# 毎月1日の予算設定

```text
📅 毎月の予算設定
[設定する]
[後でする]
```

両方に `displayText` を付けます。

---

# 機能確認

```text
機能確認 買い物リスト
機能確認 URL分類
機能確認 特売情報
```

```text
既存機能に一致 → ✅ あります + 使い方
Geminiで既存機能から対応可能 → ✅ あります + 使い方
正式ロードマップ → 🛠️ 未実装 + 予定
未対応 → 要望DB登録
```

---

# レシート系

#32 レシート入力 / #33 複数品目レシート分類は正式ロードマップから削除済みです。

---

# 新機能追加チェック

- メインメニューを不必要に長くしていないか。
- 既存カテゴリへまとめられないか。
- コマンドを知らなくても目的から到達できるか。
- 1画面1目的か。
- 危険・不可逆操作に確認があるか。
- Postbackに `displayText` が必要ではないか。
- Message actionで即時反応を出せないか。
- Postback dataが300文字以内か。
- 保存失敗時にデータを失わないか。
- README / SETUP / DEVELOPMENTを更新したか。
- UI変更ならこのファイルも更新したか。
