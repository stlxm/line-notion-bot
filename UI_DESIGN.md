# UI_DESIGN.md

LINE Notion BotのUI設計ルールです。機能追加時に画面がごちゃごちゃしないことと、「押したのに反応がないように見える」状態を減らすことを重視します。

## 基本原則

- メニューに `Phase4` / `Phase5` のような開発者向け名称を出さない。
- 実装済み機能は、ユーザーが機能名から辿れるようメニューに全て配置する。
- 機能数が多いため、1枚の縦長メニューではなくカテゴリ別Flex carouselを使う。
- 主要操作は3タップ以内。
- 通常操作はprimary、戻る・キャンセル・取り消し系はsecondary。
- 危険操作・取り消しは確認画面を挟む。
- Message actionを優先し、Postbackには原則 `displayText` を付ける。
- コマンドを覚えていることを前提にしない。

---

# メインメニュー

`メニュー` で次のカテゴリを横スワイプ表示します。

```text
✨ 案内・入口
🧾 家計簿・入力
📊 予算・分析
🎯 月次・固定費・貯金
✏️ 修正・取り消し
💳 カード
💸 貸し借り
📝 メモ・買い物
🛒 特売・あとで見る
🤖 AI
🗂 Notion・データ
```

## 案内・入口

```text
目的から探す
この機能ある？
今のおすすめ
全コマンド一覧
Notionを開く
```

## 家計簿・入力

```text
支出を入力
自然文で支出入力
よく使う支出
本日のレポート
今月の状況
```

## 予算・分析

```text
予算一覧
予算を設定
今日使える額
支出ペース
予算提案
異常支出
年間支出予測
週次レポート
```

## 月次・固定費・貯金

```text
月締め
月次レビュー
固定費一覧
固定費を追加
固定費を今月登録
貯金目標
貯金目標を追加
貯金額を更新
```

## 修正・取り消し

```text
直前登録を確認
直前登録を修正
直前登録を取り消す
```

## カード

```text
カード未処理
自動登録ルール
カードテスト
```

カード画面の説明文では、店名変更で小書き文字補正を学習することも明示します。これはボタンを増やす独立操作ではなく、既存の「店名を変更する」の中で自動判定します。

```text
サミツト → サミット
```

のように大きい仮名を対応する小書き仮名へ直しただけなら、今後の同じ明細名を自動補正し、過去の同名データもまとめて直します。別の文字変更まで含む場合は学習しません。

## 貸し借り

```text
貸した記録を追加
借りた記録を追加
貸し借り一覧
精算する
```

## メモ・買い物

```text
メモを追加
メモ一覧
メモを削除
買い物を追加
買い物リスト
購入済みにする
```

## 特売・あとで見る

```text
今日の特売
URLを保存
```

## AI

```text
AI検索の使い方
AI Lite
AI Flash
現在のAIモデル
👍 直前回答を評価
👎 直前回答を評価
直前回答を改善
DBヘルスチェック
```

## Notion・データ

```text
データを追加
Notionを開く
機能確認
全コマンド一覧
```

---

# 未認識コマンド

未認識入力のたびに大きなメニューを自動表示しません。

```text
そのコマンドはありません。
「ヘルプ」か「メニュー」と送ってください。
```

ユーザーが必要なときだけ選択画面を開きます。

---

# `コマンド`

`コマンド` は「代表例だけ」ではなく、現在実装済みのコマンドをカテゴリ別に全て載せる方針です。

新しいコマンド実装時は、コード実装と同時に `help_guide.command_list_text()` を更新します。

---

# ボタンを押した瞬間の反応

Message actionは押したテキストがすぐトークに表示されます。

Postbackは原則 `displayText` を付けます。

```text
ボタンを押す
↓
即時: 選択内容がトークへ表示
↓
数秒後: Botの処理結果
```

---

# メモ期限

期限は自然文中から認識します。

```text
メモ 住民票を明日までに提出
メモ レポートを金曜までに出す
メモ 課題を3日後までに出す
メモ 旅行準備を今週中に終わらせる
メモ 更新を今月末までにする
メモ 書類提出 9月20日まで
```

期限表現がない場合は勝手に期限を設定しません。

---

# AI評価

AI回答の直後に:

```text
[👍 良い] [👎 改善したい]
```

を表示します。

押した瞬間に `AI評価 👍` / `AI評価 👎` がトークへ表示され、評価保存へ進みます。

---

# 新機能追加チェック

- メニューに開発用Phase名を出していないか。
- 新機能をメニューから辿れるか。
- `コマンド` に新しいコマンドを追加したか。
- 未認識入力で不要な大画面を出していないか。
- Postbackに `displayText` が必要ではないか。
- Message actionで即時反応を出せないか。
- 保存失敗時にデータを失わないか。
- README / SETUP / DEVELOPMENTを更新したか。
- UI変更ならこのファイルも更新したか。
