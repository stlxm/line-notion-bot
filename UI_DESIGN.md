# UI_DESIGN.md

LINE Notion Bot のUI設計ルールです。機能追加時に画面がごちゃごちゃしないことを最優先にします。

## 1. 基本原則

- メインメニューに全機能を並べない。
- ユーザーが最初に選ぶのは「何をしたいか」というカテゴリ。
- 3タップ以内に主要操作へ到達できるようにする。
- 1画面1目的を基本にする。
- 説明文は短くし、詳細はヘルプへ逃がす。
- 同じ意味のコマンドを増やしすぎない。
- AIを使う操作と通常処理を見た目でも分ける。

## 2. 最終メインメニュー

```text
🏠 今日
💰 家計簿
💳 カード
🎯 予算・目標
📝 メモ・保存
🤖 AI
⚙️ その他
```

各ボタンからサブメニューへ進む。

## 3. サブメニュー例

### 家計簿

```text
支出を入力
最近の支出
支出を検索
今月を見る
分析
修正・取り消し
戻る
```

### カード

```text
未処理カード
最近のカード利用
自動分類ルール
まとめて処理
カード設定
戻る
```

### 予算・目標

```text
予算一覧
予算設定
使いすぎチェック
年間予測
貯金目標
月を締める
戻る
```

### メモ・保存

```text
メモ追加
メモ一覧
買い物リスト
保存URL
戻る
```

### AI

```text
AIの使い方
直前の回答を改善
最近の質問
戻る
```

## 4. 色

### primary / 緑

通常の前向き操作に使う。

- 保存
- 次へ
- ジャンル
- 支払方法
- 店名変更
- 検索
- 表示

### secondary / 色なし

終了・戻る・消極的操作。

- キャンセル
- 戻る
- 登録しない
- 今はしない

### 削除

削除候補を赤で埋め尽くさない。対象選択 → 確認画面 → 最終削除の順にする。

## 5. 列数

- ジャンルなど6文字程度以下の短い選択肢: 2列
- カード名など長さにばらつきがあるもの: 原則1列
- メニュー: 1列
- 削除候補: 1列

## 6. カード未処理

1件のカードに必ず以下を表示する。

```text
金額
利用先
カード
利用日
残り件数
```

操作:

```text
ジャンル選択
店名変更
登録しない
```

ジャンル選択は2列。店名変更はいつでも可能。

### カードジャンルの特別ルール

カード未処理画面では `固定費` は表示しない。`サブスク` は必ず表示する。

Notionの家計簿DBのジャンル選択肢に `固定費` が存在していてもUI側で除外する。逆に `サブスク` がNotionの選択肢にまだ存在しなくてもUI側で追加して表示する。

`サブスク` を選ぶと固定費DBへ登録され、今後同じカード + 同じ正規化店名のカード利用は通常検出から除外される。

保存 / 登録しない後は自動で次の未処理を表示する。

## 7. Postback data

LINEのPostback `data` は300文字以内にする。

未処理カードでは、店名・金額・カード名・日付をPostbackへ入れず `pending_id` からNotionを再取得する。

推奨:

```text
ジャンル選択 → action + pending_id + cat
店名変更     → action + pending_id
スキップ     → action + pending_id
```

## 8. 確認画面が必要な操作

- メモ削除
- 家計簿削除
- 直前登録取り消し
- 複数件一括分類
- 自動登録ルールの有効化
- 月締め確定

## 9. 通知

即時通知が必要:

- 新しいカード利用
- 高額 / 異常利用（将来）
- 保存失敗でデータ消失リスクがある場合

通知不要:

- 固定費DBで有効な同一カード + 同一正規化店名として除外されたカード利用

まとめてよい:

- 未処理件数
- 日次まとめ
- 予算状況
- メモ期限

## 10. テキスト

LINEではMarkdownを前提にしない。

```text
【見出し】
・項目
・項目
```

を基本とする。

## 11. 新機能追加チェック

- 本当にメインメニューへ追加する必要があるか
- 既存カテゴリのサブメニューに入れられないか
- 新しいコマンド名が既存と衝突しないか
- キャンセル方法があるか
- 保存失敗時にデータを失わないか
- ボタンが長すぎないか
- Postbackが300文字以内か
- 2列にして読みにくくならないか
- README / SETUP / DEVELOPMENTを更新したか
