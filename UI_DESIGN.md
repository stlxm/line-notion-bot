# UI_DESIGN.md

LINE Notion BotのUI設計ルールです。機能追加時に画面がごちゃごちゃしないことと、「押したのに反応がないように見える」状態を減らすことを重視します。

## 基本原則

- メインメニューに全機能を並べない。
- 1画面1目的を基本にする。
- 主要操作は3タップ以内。
- 通常操作はprimary、戻る・キャンセル・登録しないはsecondary。
- 危険操作・取り消しは確認画面を挟む。
- コマンドを覚えていることを前提にせず、目的から到達できる入口を用意する。

---

# ボタンを押した瞬間の反応

Message actionは押したテキストがすぐトークに表示されます。Postbackには原則 `displayText` を付けます。

```text
ボタンを押す
↓
即時: 選択内容がトークへ表示
↓
数秒後: Botの処理結果
```

Phase 5のAI評価ボタンもMessage actionです。

---

# メインメニュー

```text
✨ 迷ったら
📊 家計簿・予算
🛒 特売・買い物
✏️ 修正・取り消し
💸 貸し借り
💳 カード
📝 メモ・あとで見る
🤖 AI検索・改善
🗂 Notion・その他
```

## 特売・買い物

```text
今日の特売を見る → 特売情報
買い物リストを見る → 買い物リスト
買い物を追加する → Phase4
```

## メモ・あとで見る

```text
メモを追加する
メモ一覧を見る
Phase 4の使い方
メモを削除する
```

## AI検索・改善

```text
AI検索の使い方
👍 直前AI回答を評価 → AI評価 👍
👎 直前AI回答を評価 → AI評価 👎
Phase 5の使い方
DBヘルスチェック
AIモデルを確認
直前のAI回答を改善
```

`👍 / 👎` はMessage actionなので、タップ直後に `AI評価 👍` / `AI評価 👎` が表示されます。

---

# Phase 4 UI

## メモ期限・自動分類

```text
メモ 住民票を明日までに提出
↓
✅ メモを保存しました。
内容: 住民票を提出
分類: やること
期限: 2026-09-15
```

分類:

```text
買い物 / やること / 予定 / アイデア / その他
```

## 買い物リスト

```text
買い物 牛乳
買い物リスト
買った 牛乳
```

`買った` は1件だけ一致した場合に完了化。複数一致なら停止します。

## URL保存

```text
https://example.com/article
↓
✅ URLを保存しました。
タイトル: ページタイトル
カテゴリ: 記事
ドメイン: example.com
```

カテゴリ:

```text
記事 / 買い物 / 動画 / SNS / 資料 / その他
```

---

# Phase 5 UI

## AI回答

AI本文の後ろに必ずシステム側で参照情報を追加します。

```text
AI回答本文

【参照DB】
・家計簿
・月別管理

【根拠】
参照DBから取得した実データ、質問の期間指定に合わせたデータ

評価する場合: AI評価 👍 / AI評価 👎
```

参照DB名はGeminiの自己申告ではなく、実際に `notion_helper` が文脈として選択したDB名から生成します。

## AI評価

現時点ではAI回答直下にFlexボタンを差し込む方式ではありません。メインメニューの2ボタン、またはコマンドで評価します。

```text
AI評価 👍
AI評価 👎
```

評価後:

```text
👍 → AI改善ログへ評価保存
👎 → AI改善ログへ評価保存 + 「AI改善」の案内
```

## AI改善

`AI改善` でユーザーが期待する回答を保存した場合、次回以降の似た質問で回答例として参照します。Phase 5ではさらに、そのログに保存された参照DB名をDBルーターの候補へ利用します。

## DBヘルスチェック

```text
DBヘルスチェック
```

表示例:

```text
🩺 Notion DBヘルスチェック
正常: 10 / 12

✅ 家計簿
✅ メモ
⚠️ URL: 必須列不足 → カテゴリ
❌ 貸し借り: http_404 / ...
```

秘密値は表示しません。

---

# Phase 3A / 3B UI

自然文家計簿は不足項目を選択後に確認して保存。直前取り消しは確認必須です。

---

# カード・予算

カードPostbackには `displayText` を付けます。毎月1日の予算設定ボタンも同様です。

---

# 機能確認

```text
機能確認 買い物リスト
機能確認 URL分類
機能確認 AI評価
機能確認 DBヘルス
```

```text
既存機能に一致 → ✅ あります + 使い方
Geminiで既存機能から対応可能 → ✅ あります + 使い方
正式ロードマップ → 🛠️ 未実装 + 予定
未対応 → 要望DB登録
```

---

# レシート系

#32 レシート入力 / #33 複数品目レシート分類は正式ロードマップから削除済みです。

---

# 新機能追加チェック

- メインメニューを不必要に長くしていないか。
- 既存カテゴリへまとめられないか。
- コマンドを知らなくても目的から到達できるか。
- 危険・不可逆操作に確認があるか。
- Postbackに `displayText` が必要ではないか。
- Message actionで即時反応を出せないか。
- 保存失敗時にデータを失わないか。
- README / SETUP / DEVELOPMENTを更新したか。
- UI変更ならこのファイルも更新したか。
