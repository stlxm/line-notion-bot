# UI_DESIGN.md

LINE Notion Bot のUI設計ルールです。機能追加時に画面がごちゃごちゃしないことと、LINE Flex Messageの制約を超えないことを最優先にします。

## 1. 基本原則

- メインメニューに全機能を並べない。
- ユーザーが最初に選ぶのは「何をしたいか」というカテゴリ。
- 3タップ以内に主要操作へ到達できるようにする。
- 1画面1目的を基本にする。
- 説明文は短くし、詳細はヘルプへ逃がす。
- 同じ意味のコマンドを増やしすぎない。
- AIを使う操作と通常処理を見た目でも分ける。

## 2. 最終メインメニュー

```text
🏠 今日
💰 家計簿
💳 カード
🎯 予算・目標
📝 メモ・保存
🤖 AI
⚙️ その他
```

各ボタンからサブメニューへ進む。

## 3. 色

- 通常の前向き操作: `primary` / 緑
- キャンセル・戻る・登録しない: `secondary` / 色なし
- 削除: 確認画面を挟み、通常画面では強調しすぎない

## 4. 列数

- ジャンルなど6文字程度以下の短い選択肢: 2列
- カード名など長さにばらつきがあるもの: 原則1列
- メニュー: 1列
- 削除候補: 1列

## 5. カード未処理

1件のカードに以下を表示する。

```text
金額
利用先
カード
利用日
残り件数
```

操作:

```text
ジャンル選択
店名変更
登録しない
```

ジャンル選択は2列。店名変更はいつでも可能。保存 / 登録しない後は自動で次の未処理を表示する。

## 6. LINE Postback data の300文字上限

LINEの `PostbackAction.data` は300文字以内にする必要がある。

### 禁止する設計

未処理カードの各ボタンへ次を全部埋め込まない。

```text
card
store
amount
date
category
pending_id
```

日本語店名はURLエンコードすると文字数が増え、300文字を超える可能性がある。

### 採用する設計

Notionなど永続ストレージにデータがある場合は、Postbackへ識別子だけ送る。

未処理カードのジャンル:

```text
action=kakeibo_save
pending_id=<Notion page id>
cat=<ジャンル>
```

店名変更:

```text
action=card_change_store_start
pending_id=<Notion page id>
```

受信後に `card_queue.get_item(pending_id)` でカード名・店名・金額・日付を再取得する。

この方式を今後のメモ・家計簿・一括処理などにも優先する。

## 7. 確認画面が必要な操作

- メモ削除
- 家計簿削除
- 直前登録取り消し
- 複数件一括分類
- 自動登録ルールの有効化
- 月締め確定

## 8. 通知

即時通知:

- 新しいカード利用
- 高額 / 異常利用（将来）
- 保存失敗でデータ消失リスクがある場合

まとめてよいもの:

- 未処理件数
- 日次まとめ
- 予算状況
- メモ期限

## 9. テキスト

LINEではMarkdownを前提にしない。

```text
【見出し】
・項目
・項目
```

を基本とする。

## 10. 新機能追加チェック

- 本当にメインメニューへ追加する必要があるか
- 既存カテゴリのサブメニューに入れられないか
- 新しいコマンド名が既存と衝突しないか
- キャンセル方法があるか
- 保存失敗時にデータを失わないか
- ボタンが長すぎないか
- Postback `data` が300文字以内か
- 永続データをPostbackへ重複格納していないか
- 2列にして読みにくくならないか
- README / SETUP / DEVELOPMENTを更新したか
