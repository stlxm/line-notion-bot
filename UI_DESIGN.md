# UI_DESIGN.md

LINE Notion BotのUI設計ルールです。機能追加時に画面がごちゃごちゃしないことを最優先にします。

## 基本原則

- メインメニューに全機能を並べない。
- 1画面1目的を基本にする。
- 主要操作は3タップ以内。
- 通常操作は緑、戻る・キャンセル・登録しないは控えめ。
- 長い選択肢は1列、短いジャンルは2列。
- Notionに実データがある場合、Postbackへ値を詰め込まずIDから再取得する。
- 大きい機能群は「入口1つ + 短いコマンド」で整理する。
- コマンドを覚えていることを前提にせず、目的から到達できる入口を用意する。

## メインメニュー

現在は機能群ごとのセクションで整理します。

最上部に「迷ったら」を置きます。

```text
目的から探す → ？
今のおすすめ → おすすめ
この機能ある？ → 機能確認 ○○
全コマンド一覧 → コマンド
```

家計簿・予算の入口:

```text
今月のダッシュボード
支出を入力する
家計判断メニュー
今日使える額
支出ペースを見る
月次レビュー
貸し借り一覧
```

Phase 2の全機能をメインメニューへ直接並べず、`家計判断`で2A / 2B / 2Cの一覧を表示します。

カード入口:

```text
カード未処理を確認
自動分類ルールを見る
```

将来の最終カテゴリ構成:

```text
🏠 今日
💰 家計簿
💳 カード
🎯 予算・目標
📝 メモ・保存
🤖 AI
⚙️ その他
```

## 目的ベースのヘルプ

`？` または `ヘルプ` で、長いコマンド一覧ではなく目的を選ぶFlexを表示します。

```text
💰 お金を確認したい
📝 記録したい
💳 カードを整理したい
🎯 予算・目標を考えたい
🤖 AIに聞きたい
⚙️ その他
```

各ボタンは通常操作なのでprimary / 緑。

下部に:

```text
今のおすすめを見る → primary
全コマンドを見る → secondary
```

を置きます。

### 「この機能ある？」

機能数が増えたため、ユーザーがメニューを探し回らずに質問できる入口を用意します。

推奨入力:

```text
機能確認 レシート入力
レシート入力ってできる？
貸し借りってある？
```

応答ルール:

```text
実装済み
→ ✅ あります + 使い方

ロードマップ済み・未実装
→ 🛠️ まだ使えない + 実装予定フェーズ

未知の機能
→ 未実装と案内 + 機能追加要望DBへ自動記録
```

ユーザーに「要望登録しますか？」と毎回確認させず、未知機能だけ自動記録します。ロードマップ済みの機能は重複要望を作りません。

## Phase 2 — 家計判断

```text
2A 日々の判断
・今日使える
・ペース
・異常支出

2B 月次判断
・予算提案
・月締め
・月次レビュー

2C 将来予測・目標
・年間予測
・貯金目標

2.9 貸し借り
・貸した
・借りた
・貸し借り一覧
・精算
```

### 貸し借りUIルール

入力は短い自然なコマンドを使います。

```text
貸した 田中 3000 ランチ代
借りた 田中 2000
貸し借り一覧
精算 田中 3000
```

安全ルール:
- 貸し借りDBは家計簿と分離し、自動で支出・収入へ計上しない。
- `精算` は相手+金額で未精算1件だけ一致するときだけ更新する。
- 同額の候補が複数ある場合は勝手に1件選ばない。
- 一覧は未精算を優先して表示する。

### 毎月1日の予算設定案内

毎月1日朝6時台に送るFlexは、1画面1目的で次の2択だけにします。

```text
📅 毎月の予算設定
今月の全体予算を設定しますか？

[設定する] primary
[後でする] secondary
```

Postback:

```text
設定する → action=start_monthly_budget_input
後でする → action=cancel_registration
```

ルール:
- 金額入力欄やジャンル設定を同じFlexへ詰め込まない。
- `設定する` 後に数字入力へ進む。
- `後でする` はその場で終了し、Notionを変更しない。
- 毎月の自動通知なので、ボタン数を増やさない。

### 月締めUIルール

```text
月締め YYYY-MM
↓
プレビュー
↓
月締め確定 YYYY-MM
```

- 今月・未来月は締め不可。
- 通常確定はカード未処理があると停止。
- 未処理を確認済みで意図的に締める場合だけ`月締め確定強制 YYYY-MM`。
- プレビューではNotionを書き換えない。

### 月次AIレビュー

- LINEへ先に「生成中」を返す。
- Gemini処理はバックグラウンドで実行。
- 完了後にPushする。
- 現在選択中のLite / Flashを明記する。

## 色

primary / 緑:
- 保存
- 次へ
- ジャンル
- 店名変更
- まとめて処理
- 自動登録ON
- 家計判断など通常の入口
- 目的別ヘルプ
- おすすめ
- 毎月予算の「設定する」

secondary / 控えめ:
- キャンセル
- 戻る
- 登録しない
- 自動登録OFF
- 重複として処理済みにする
- 全コマンド一覧など補助的な入口
- 危険操作を回避する選択肢
- 毎月予算の「後でする」

## カード未処理

表示:

```text
金額
利用先
カード
利用日
おすすめジャンル（学習済みなら）
同じ店の未処理件数（2件以上なら）
```

ジャンル:
- 2列。
- おすすめは`★ジャンル`として先頭。
- `固定費`は表示しない。
- `サブスク`は必ず表示。

追加操作:

```text
同じ店 N件をまとめる
店名を変更する
次回から自動登録ON
登録しない
```

## 同じ店まとめ処理

`同じ店 N件をまとめる`を押した後は、同じ2列ジャンルUIを再利用します。誤操作を防ぐため、戻るボタンを表示します。

一括処理中に家計簿重複候補が見つかった項目は勝手に削除せず未処理へ残します。

## 重複確認

```text
⚠️ 重複候補があります
既存データ概要

[それでも保存する]          primary
[重複として処理済みにする] secondary
```

同じ条件でも正当な複数利用があり得るため、ユーザー確認を優先します。

## 自動登録ルール画面

コマンド:

```text
カード自動登録
```

3回以上100%一致したルールだけ`自動登録ON`を表示します。

## Postback data

LINEのPostback `data` は300文字以内。

未処理カード:

```text
通常保存       → action + pending_id + cat
一括分類       → action + pending_id + cat
店名変更       → action + pending_id
自動登録ON/OFF → action + rule_id
重複確認       → action + pending_id + cat（必要な場合）
```

毎月予算:

```text
設定する → action=start_monthly_budget_input
後でする → action=cancel_registration
```

貸し借り・機能確認は現時点ではテキストコマンドで処理し、Postbackを増やしません。

## 通知

即時:
- 通常の新規カード利用。
- 安全な自動登録が成功したカード利用。
- 保存失敗でデータ消失リスクがある場合。

定期:
- 毎月1日朝6時台の予算設定案内。
- 未処理件数の日次通知。
- 月末の未処理ゼロ/残件数通知。

### サミット特売通知

```text
🔥 今日・短期特売
・1〜7日間の特売

📅 月間・長期特売
・8日以上の特売
```

通知前に表記揺れ重複を整理し、短期欄を先に表示します。

## 新機能追加チェック

- メインメニューを不必要に長くしていないか。
- 既存カテゴリへまとめられないか。
- コマンドを知らなくても目的から到達できるか。
- `機能確認 ○○` のカタログへ追加したか。
- 未知の機能要望DBへの重複登録を防いでいるか。
- 大きい機能群はA/B/Cへ分割できないか。
- 1画面1目的か。
- 危険・不可逆操作に確認があるか。
- Postbackが300文字以内か。
- 保存失敗時にデータを失わないか。
- README / SETUP / DEVELOPMENTを更新したか。
- UI変更ならこのファイルも更新したか。
