# UI_DESIGN.md

LINE Notion BotのUI設計ルールです。機能追加時に画面がごちゃごちゃしないことと、「押したのに反応がないように見える」状態を減らすことを重視します。

## 基本原則

- メインメニューに全機能を並べない。
- 1画面1目的を基本にする。
- 主要操作は3タップ以内。
- 通常操作はprimary、戻る・キャンセル・登録しないはsecondary。
- 長い選択肢は1列、短いジャンルは2列。
- Notionに実データがある場合、Postbackへ値を詰め込みすぎずIDから再取得する。
- 危険操作・取り消しは対象を表示して確認画面を必ず挟む。
- コマンドを覚えていることを前提にせず、目的から到達できる入口を用意する。

---

# ボタンを押した瞬間の反応

## Message action

Message actionはLINE側で押したテキストがそのままトークに表示されるため、ユーザーはタップできたことをすぐ確認できます。

メニューとPhase 3Aの選択ボタンでは可能な限りMessage actionを使います。

## Postback action

Postbackはそのままだとトークに何も表示されず、Renderの応答待ち中に「押せたか分からない」状態になります。そのためPostbackには原則 `displayText` を付けます。

期待する見え方:

```text
ボタンを押す
↓
即時: ▶ 食費
↓
数秒後: Botの処理結果
```

現在の主な対象:

- メニュー内のPostback
- ジャンル選択
- 支払方法選択
- カード分類
- 同じ店まとめ処理
- 店名変更
- 重複確認
- 自動登録ON/OFF
- 毎月1日の予算設定

---

# メインメニュー

現在の主要カテゴリ:

```text
✨ 迷ったら
📊 家計簿・予算
✏️ 修正・取り消し
💸 貸し借り
💳 カード
📝 メモ
🤖 AI検索・改善
🗂 Notion・その他
```

## 家計簿・予算

```text
本日のレポート
今月のダッシュボード
支出を入力する
自然文で支出入力
よく使う支出
家計判断メニュー
今日使える額
支出ペースを見る
月次レビュー
予算一覧を見る
週次レポートを見る
固定費一覧を見る
```

`自然文で支出入力` は使い方を表示し、実際の入力はそのまま普通の文章で送ります。

`よく使う支出` は直近90日の家計簿から頻出の店・ジャンル・支払方法を候補化します。

---

# Phase 3A UI

## 自然文家計簿

入力例:

```text
今日サミットで2380円使った
昨日コンビニで540円買った
```

流れ:

```text
自然文を送信
↓
金額・店名・日付を解析
↓
ジャンル不明ならジャンル選択
↓
支払方法不明なら支払方法選択
↓
登録内容を一覧表示
↓
[この内容で登録]
[キャンセル]
```

選択ボタンはMessage actionを使うため、タップ直後に `自然ジャンル 食費` / `自然支払 JCB` などがトークへ表示されます。

最終確認を通るまではNotionへ保存しません。

## よく使う支出テンプレート

```text
支出テンプレート
↓
[サミット ¥x,xxx]
[コンビニ ¥xxx]
...
↓
登録内容確認
↓
[この内容で登録]
```

テンプレート金額は直近90日の同一 `店名 + ジャンル + 支払方法` の中央値です。ボタンを押しただけでは保存せず、確認画面を挟みます。

## 本日のレポート

メニュー上部の家計簿セクションに置きます。

表示内容:

```text
本日の支出合計・件数
ジャンル別内訳
最大支出
今月累計
残り予算
今日以降の1日あたり目安
カード未処理件数
```

日常利用頻度が高いので、月次レポートより上に配置します。

---

# Phase 3B UI

## 直前登録表示

表示:

```text
日付
店名
金額
ジャンル
支払方法
```

その下に修正項目を並べます。

```text
金額を修正
店名を修正
日付を修正
ジャンルを修正
支払方法を修正
直前登録を取り消す
```

## 取り消し

```text
直前取り消し
↓
対象を表示
↓
[取り消す]
[やめる]
```

`取り消す` はNotionページをアーカイブします。物理削除しません。

---

# ジャンル別予算

```text
予算 お菓子 3000
```

月別管理DBに `お菓子予算` がない場合は自動作成して保存します。成功時は、列を自動追加したこともLINEへ表示します。

---

# カード未処理

表示:

```text
金額
利用先
カード
利用日
おすすめジャンル
同じ店の未処理件数
```

ジャンル:
- 2列。
- おすすめは `★ジャンル`。
- `固定費`は表示しない。
- `サブスク`は必ず表示。
- Postback選択時は `displayText` で即時反応。

---

# 毎月1日の予算設定

```text
📅 毎月の予算設定
今月の全体予算を設定しますか？

[設定する]
[後でする]
```

両方に `displayText` を付けます。

---

# 機能確認

```text
機能確認 貸し借り
機能確認 自然文家計簿
機能確認 本日のレポート
```

表示方針:

```text
既存機能に一致 → ✅ あります + 使い方
Geminiで既存機能から対応可能 → ✅ あります + 使い方
正式ロードマップ → 🛠️ 未実装 + 予定
未対応 → 要望DB登録
```

---

# レシート系

ユーザー判断により #32 レシート入力 / #33 複数品目レシート分類は正式ロードマップから削除済みです。メニュー・ヘルプ・機能案内で「予定機能」として表示しません。

---

# 新機能追加チェック

- メインメニューを不必要に長くしていないか。
- 既存カテゴリへまとめられないか。
- コマンドを知らなくても目的から到達できるか。
- 1画面1目的か。
- 危険・不可逆操作に確認があるか。
- Postbackに `displayText` が必要ではないか。
- Message actionで即時反応を出せないか。
- Postback dataが300文字以内か。
- 保存失敗時にデータを失わないか。
- README / SETUP / DEVELOPMENTを更新したか。
- UI変更ならこのファイルも更新したか。
