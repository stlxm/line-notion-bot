# UI_DESIGN.md

LINE Notion BotのUI設計ルールです。機能追加時に画面がごちゃごちゃしないことと、「押したのに反応がないように見える」状態を減らすことを重視します。

## 基本原則

- メインメニューに全機能を並べない。
- 1画面1目的を基本にする。
- 主要操作は3タップ以内。
- 通常操作はprimary、戻る・キャンセル・登録しないはsecondary。
- 長い選択肢は1列、短いジャンルは2列。
- Notionに実データがある場合、Postbackへ値を詰め込みすぎずIDから再取得する。
- 危険操作・取り消しは対象を表示して確認画面を必ず挟む。
- コマンドを覚えていることを前提にせず、目的から到達できる入口を用意する。

---

# ボタンを押した瞬間の反応

## Message action

Message actionはLINE側で押したテキストがそのままトークに表示されるため、ユーザーはタップできたことをすぐ確認できます。

メニューでは可能な限りMessage actionを使います。

## Postback action

Postbackはそのままだとトークに何も表示されず、Renderの応答待ち中に「押せたか分からない」状態になります。

そのためPostbackには原則 `displayText` を付けます。

例:

```json
{
  "type": "postback",
  "label": "食費",
  "data": "action=...",
  "displayText": "▶ 食費"
}
```

期待する見え方:

```text
ボタンを押す
↓
即時: ▶ 食費
↓
数秒後: Botの処理結果
```

現在の主な対象:

- メニュー内のPostback
- ジャンル選択
- 支払方法選択
- カード分類
- 同じ店まとめ処理
- 店名変更
- 重複確認
- 自動登録ON/OFF
- 毎月1日の予算設定

`displayText` はユーザー向け表示だけで、Bot側の分岐には `data` を使います。

---

# メインメニュー

現在の主要カテゴリ:

```text
✨ 迷ったら
📊 家計簿・予算
✏️ 修正・取り消し
💸 貸し借り
💳 カード
📝 メモ
🤖 AI検索・改善
🗂 Notion・その他
```

## 迷ったら

```text
目的から探す → ？
この機能ある？ → 機能確認
今のおすすめ → おすすめ
全コマンド一覧 → コマンド
```

`機能確認` だけ送った場合は、Botが確認したい機能名の入力例を返します。

## 家計簿・予算

```text
今月のダッシュボード
支出を入力する
家計判断メニュー
今日使える額
支出ペースを見る
月次レビュー
予算一覧を見る
週次レポートを見る
固定費一覧を見る
```

## 修正・取り消し

```text
直前登録を確認・修正 → 直前登録
直前登録を取り消す   → 直前取り消し
```

`直前登録` の画面では、最新1件を表示してから次を選べます。

```text
金額を修正
店名を修正
日付を修正
ジャンルを修正
支払方法を修正
直前登録を取り消す
```

取り消しは必ず確認画面を挟みます。

## 貸し借り

```text
貸し借り一覧
貸し借りの使い方
```

新規登録は自然な短いコマンドを使います。

```text
貸した 田中 3000 ランチ代
借りた 田中 2000
```

---

# Phase 3B UI

## 直前登録表示

表示:

```text
日付
店名
金額
ジャンル
支払方法
```

その下に修正項目を並べます。

## 修正

現在はボタンで修正対象を選び、続けて新しい値を送る方式です。

例:

```text
[金額を修正]
↓
直前修正 金額
↓
Bot: 例: 直前修正 金額 1500
```

最終更新コマンド:

```text
直前修正 金額 1500
直前修正 店名 サミット
直前修正 日付 2026-09-12
直前修正 ジャンル 食費
直前修正 支払方法 JCB
```

## 取り消し

```text
直前取り消し
↓
対象を表示
↓
[取り消す]
[やめる]
```

`取り消す` はNotionページをアーカイブします。物理削除しません。

---

# カード未処理

表示:

```text
金額
利用先
カード
利用日
おすすめジャンル
同じ店の未処理件数
```

ジャンル:
- 2列。
- おすすめは `★ジャンル`。
- `固定費`は表示しない。
- `サブスク`は必ず表示。
- Postback選択時は `displayText` で即時反応。

---

# 重複確認

```text
⚠️ 重複候補があります
既存データ概要

[それでも保存する]
[重複として処理済みにする]
```

同じ条件でも正当な複数利用があり得るため、ユーザー確認を優先します。

---

# 毎月1日の予算設定

```text
📅 毎月の予算設定
今月の全体予算を設定しますか？

[設定する]
[後でする]
```

Postback:

```text
設定する → action=start_monthly_budget_input
後でする → action=cancel_registration
```

両方に `displayText` を付けます。

---

# 機能確認

```text
機能確認 貸し借り
機能確認 家計簿を後から直す
```

表示方針:

```text
既存機能に一致 → ✅ あります + 使い方
Geminiで既存機能から対応可能 → ✅ あります + 使い方
正式ロードマップ → 🛠️ 未実装 + 予定
未対応 → 要望DB登録
```

---

# レシート系

ユーザー判断により #32 レシート入力 / #33 複数品目レシート分類は正式ロードマップから削除済みです。メニュー・ヘルプ・機能案内で「予定機能」として表示しません。

---

# 新機能追加チェック

- メインメニューを不必要に長くしていないか。
- 既存カテゴリへまとめられないか。
- コマンドを知らなくても目的から到達できるか。
- 1画面1目的か。
- 危険・不可逆操作に確認があるか。
- Postbackに `displayText` が必要ではないか。
- Postback dataが300文字以内か。
- 保存失敗時にデータを失わないか。
- README / SETUP / DEVELOPMENTを更新したか。
- UI変更ならこのファイルも更新したか。
