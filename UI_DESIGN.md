# UI_DESIGN.md

LINE Notion BotのUI設計ルールです。機能追加時に画面がごちゃごちゃしないことを最優先にします。

## 基本原則

- メインメニューに全機能を並べない。
- 1画面1目的を基本にする。
- 主要操作は3タップ以内。
- 通常操作は緑、戻る・キャンセル・登録しないは控えめ。
- 長い選択肢は1列、短いジャンルは2列。
- Notionに実データがある場合、Postbackへ値を詰め込まずIDから再取得する。
- 大きい機能群は「入口1つ + 短いコマンド」で整理する。

## メインメニュー

現在は機能群ごとのセクションで整理します。

家計簿・予算の入口:

```text
今月のダッシュボード
支出を入力する
家計判断メニュー
今日使える額
支出ペースを見る
月次レビュー
```

Phase 2の全機能をメインメニューへ直接並べず、`家計判断`で2A / 2B / 2Cの一覧を表示します。

カード入口:

```text
カード未処理を確認
自動分類ルールを見る
```

将来の最終カテゴリ構成:

```text
🏠 今日
💰 家計簿
💳 カード
🎯 予算・目標
📝 メモ・保存
🤖 AI
⚙️ その他
```

## Phase 2 — 家計判断

Phase 2は情報量が多いため、3ブロックで案内します。

```text
2A 日々の判断
・今日使える
・ペース
・異常支出

2B 月次判断
・予算提案
・月締め
・月次レビュー

2C 将来予測・目標
・年間予測
・貯金目標
```

コマンド:

```text
家計判断
```

で一覧を確認できます。

### 月締めUIルール

月締めは不可逆に近い管理操作なので安全性を優先します。

```text
月締め YYYY-MM
↓
プレビュー
↓
月締め確定 YYYY-MM
```

- 今月・未来月は締め不可。
- 通常確定はカード未処理があると停止。
- 未処理を確認済みで意図的に締める場合だけ`月締め確定強制 YYYY-MM`。
- プレビューではNotionを書き換えない。

### 月次AIレビュー

- LINEへ先に「生成中」を返す。
- Gemini処理はバックグラウンドで実行。
- 完了後にPushする。
- 現在選択中のLite / Flashを明記する。

## 色

primary / 緑:
- 保存
- 次へ
- ジャンル
- 店名変更
- まとめて処理
- 自動登録ON
- 家計判断など通常の入口

secondary / 控えめ:
- キャンセル
- 戻る
- 登録しない
- 自動登録OFF
- 重複として処理済みにする
- 危険操作を回避する選択肢

## カード未処理

表示:

```text
金額
利用先
カード
利用日
おすすめジャンル（学習済みなら）
同じ店の未処理件数（2件以上なら）
```

ジャンル:
- 2列。
- おすすめは`★ジャンル`として先頭。
- `固定費`は表示しない。
- `サブスク`は必ず表示。

追加操作:

```text
同じ店 N件をまとめる
店名を変更する
次回から自動登録ON
登録しない
```

## 同じ店まとめ処理

`同じ店 N件をまとめる`を押した後は、同じ2列ジャンルUIを再利用します。誤操作を防ぐため、戻るボタンを表示します。

一括処理中に家計簿重複候補が見つかった項目は勝手に削除せず未処理へ残します。

## 重複確認

家計簿に同日・同額・同カード・同じ正規化店名のデータがある場合:

```text
⚠️ 重複候補があります
既存データ概要

[それでも保存する]          primary
[重複として処理済みにする] secondary
```

同じ条件でも正当な複数利用があり得るため、ユーザー確認を優先します。

## 自動登録ルール画面

コマンド:

```text
カード自動登録
```

表示:

```text
店名 → ジャンル / ON・OFF / 学習状況
```

3回以上100%一致したルールだけ`自動登録ON`を表示します。ON済みには`自動登録OFF`を表示します。

## Postback data

LINEのPostback `data` は300文字以内。

未処理カード:

```text
通常保存       → action + pending_id + cat
一括分類       → action + pending_id + cat
店名変更       → action + pending_id
自動登録ON/OFF → action + rule_id
重複確認       → action + pending_id + cat（必要な場合）
```

店名・金額・カード名・利用日はPostbackへ入れずNotionから再取得します。

## 通知

即時:
- 通常の新規カード利用。
- 安全な自動登録が成功したカード利用。
- 保存失敗でデータ消失リスクがある場合。

通知しない:
- 固定費DBで有効なサブスク除外ルールに一致したカード利用。

定期:
- 未処理件数の日次通知。
- 月末の未処理ゼロ/残件数通知。

## 新機能追加チェック

- メインメニューを不必要に長くしていないか。
- 既存カテゴリへまとめられないか。
- 大きい機能群はA/B/Cへ分割できないか。
- 1画面1目的か。
- 危険・不可逆操作に確認があるか。
- Postbackが300文字以内か。
- 保存失敗時にデータを失わないか。
- README / SETUP / DEVELOPMENTを更新したか。
- UI変更ならこのファイルも更新したか。
