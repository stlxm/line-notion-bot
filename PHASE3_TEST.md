# PHASE3_TEST.md

Phase 3A（自然文家計簿・支出テンプレート）、Phase 3B（直前登録の修正・取り消し）、本日のレポート、ジャンル予算修正、ボタン即時反応の実機確認手順です。

状態: **実装済み・要実機確認**

---

# 0. 事前準備

GitHub最新版をRenderへ再デプロイします。

追加のNotion DBやRender環境変数は不要です。既存の次を使います。

```text
NOTION_API_KEY
NOTION_KAKEIBO_DATABASE_ID
NOTION_MONTHLY_DATABASE_ID
NOTION_CARD_PENDING_DATABASE_ID
```

---

# 1. メニュー確認

LINE:

```text
メニュー
```

確認:

```text
📊 家計簿・予算
  本日のレポート
  自然文で支出入力
  よく使う支出

✏️ 修正・取り消し
  直前登録を確認・修正
  直前登録を取り消す
```

が表示されること。

---

# 2. Phase 3A — 自然文家計簿

まず使い方案内:

```text
自然文入力
```

次に:

```text
今日Phase3Aテスト店で123円使った
```

期待:
- 金額 `123`
- 店名 `Phase3Aテスト店`
- 日付が今日
- ジャンルが推定できなければジャンル選択ボタン
- 支払方法が文章にないので支払方法選択ボタン
- 最後に登録内容確認
- `この内容で登録` を押すまでNotionへ保存されない

`キャンセル` した場合、Notionへ登録されないことも確認します。

次に昨日判定:

```text
昨日コンビニで540円買った
```

日付が昨日になることを確認します。

---

# 3. Phase 3A — 支出テンプレート

ある程度家計簿データがある状態で:

```text
支出テンプレート
```

期待:
- 直近90日から頻出候補が最大6件表示される
- 店名と目安金額が表示される
- 候補を押すと今日の日付で確認画面が出る
- いきなり保存されない
- `この内容で登録` 後だけNotionへ保存される

データが少ない場合は「候補を作れませんでした」と安全に返ればOKです。

---

# 4. 「予算 お菓子 3000」回帰テスト

LINE:

```text
予算 お菓子 3000
```

期待:
- 月別管理DBに `お菓子予算` がなければNumber列を自動作成
- 当月の `お菓子予算` が3000になる
- LINEに設定成功が返る

続けて:

```text
予算一覧
```

で、お菓子予算が表示されることを確認します。

同じ列がすでにある状態で再度実行しても、重複列を作らず値だけ更新されること。

---

# 5. 本日のレポート

LINE:

```text
本日のレポート
```

期待:
- 今日の支出合計と件数
- ジャンル別内訳
- 今日の最大支出
- 今月累計
- 全体予算があれば残り予算
- 1日あたり目安
- カード未処理件数

が表示されること。

別名も確認:

```text
今日のレポート
日次レポート
```

---

# 6. ボタン即時反応

ボタンを押した直後、Botの応答より先にトーク画面へ選択内容が出ることを確認します。

Phase 3A:

```text
自然ジャンル 食費
自然支払 JCB
自然登録する
```

既存Postback:

```text
▶ 食費
▶ 店名を変更する
▶ 登録しない
```

「押してから何も見えない時間」がなくなればOKです。

---

# 7. Phase 3B — 直前登録の確認

テスト用家計簿を1件作ります。

```text
支出 123 テスト用
```

保存後:

```text
直前登録
```

期待:
- 最新の家計簿1件が表示される
- 日付 / 店名 / 金額 / ジャンル / 支払方法が合っている
- 修正ボタンと取り消しボタンが出る

---

# 8. Phase 3B — 修正

```text
直前修正 金額 456
直前修正 店名 Phase3Bテスト
直前修正 日付 2026-09-12
直前修正 ジャンル 食費
直前修正 支払方法 現金
```

各操作後、Notionの同じ行が更新されることを確認します。

不正日付:

```text
直前修正 日付 9/12
```

では更新されないこと。

---

# 9. Phase 3B — 取り消し安全確認

```text
直前取り消し
```

期待:
- 対象レコードが表示される
- この時点ではNotionはまだ変わらない
- `取り消す` / `やめる` が表示される

一度 `やめる` を押してデータが残ることを確認後、再度 `直前取り消し` → `取り消す` を実行します。

確認:
- LINEに完了が返る
- Notionでは物理削除ではなくアーカイブ
- 次の `直前登録` は次に新しい未アーカイブ行を表示

---

# 10. 完了条件

```text
Phase 3A 自然文入力 OK
Phase 3A ジャンル選択 OK
Phase 3A 支払方法選択 OK
Phase 3A 確認前に保存されない OK
Phase 3A テンプレート OK
予算 お菓子 3000 OK
本日のレポート OK
メニュー追加 OK
ボタン即時表示 OK
Phase 3B 各修正 OK
Phase 3B 取り消し安全確認 OK
```

ここまで通ればPhase 3A / 3Bを `実装完了・実機確認済み` に更新します。

---

# レシート機能について

#32 レシート入力 / #33 複数品目レシート分類はユーザー判断で正式ロードマップから削除済みです。このテストには含めません。
