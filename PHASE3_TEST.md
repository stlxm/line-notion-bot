# PHASE3_TEST.md

Phase 3B（直前登録の修正・取り消し）と、メニュー/ボタン即時反応の実機確認手順です。

状態: **実装済み・要実機確認**

---

# 0. 事前準備

GitHub最新版をRenderへ再デプロイします。

追加のNotion DBやRender環境変数は不要です。既存の次を使います。

```text
NOTION_API_KEY
NOTION_KAKEIBO_DATABASE_ID
```

テストは実データではなく、テスト用家計簿を1件作ってから行うのがおすすめです。

例:

```text
支出 123 テスト用
```

ジャンル・支払方法を選択して保存します。

---

# 1. メニュー確認

LINE:

```text
メニュー
```

確認:

```text
✨ 迷ったら
  この機能ある？

✏️ 修正・取り消し
  直前登録を確認・修正
  直前登録を取り消す

💸 貸し借り
  貸し借り一覧
  貸し借りの使い方
```

が表示されること。

---

# 2. ボタン即時反応

Postbackボタンを押した直後、Botの応答より先にトーク画面へ次のような表示が出ることを確認します。

```text
▶ 食費
▶ 店名を変更する
▶ 登録しない
```

確認対象:
- 支出入力後のジャンル選択
- 支払方法選択
- カード未処理の分類ボタン
- 毎月1日の予算設定Flex

「押してから何も見えない時間」がなくなればOKです。

---

# 3. 直前登録の確認

LINE:

```text
直前登録
```

期待:
- 最新の家計簿1件が表示される
- 日付 / 店名 / 金額 / ジャンル / 支払方法が合っている
- 金額 / 店名 / 日付 / ジャンル / 支払方法の修正ボタンが出る
- 取り消しボタンが出る

---

# 4. 金額修正

LINE:

```text
直前修正 金額 456
```

確認:
- LINEに更新成功が返る
- Notion家計簿の同じ行が456円へ変わる

テスト後、必要なら元の金額へ戻します。

---

# 5. 店名修正

```text
直前修正 店名 Phase3Bテスト
```

確認:
- `内容・店名` が更新される

---

# 6. 日付修正

```text
直前修正 日付 2026-09-12
```

確認:
- `日付` が更新される
- 不正形式では更新されない

不正例:

```text
直前修正 日付 9/12
```

---

# 7. ジャンル・支払方法修正

```text
直前修正 ジャンル 食費
直前修正 支払方法 現金
```

確認:
- Selectプロパティが更新される
- Notion側に存在する選択肢を使う

---

# 8. 取り消し安全確認

まず:

```text
直前取り消し
```

確認:
- 対象レコードが表示される
- この時点ではNotionはまだ変わらない
- `取り消す` / `やめる` が表示される

一度 `やめる` を押し、データが残っていることを確認します。

その後テスト用レコードで再度:

```text
直前取り消し
```

から `取り消す` を押します。

確認:
- LINEに取り消し完了が返る
- Notionでは物理削除ではなくアーカイブされる
- 次に `直前登録` を送ると、その次に新しい未アーカイブの家計簿が表示される

---

# 9. 完了条件

```text
メニュー追加 OK
Postback即時表示 OK
直前登録表示 OK
金額修正 OK
店名修正 OK
日付修正 OK
ジャンル修正 OK
支払方法修正 OK
取り消し確認画面 OK
キャンセル時に変更なし OK
確定時にアーカイブ OK
```

ここまで通ればPhase 3Bを `実装完了・実機確認済み` に更新します。

---

# レシート機能について

#32 レシート入力 / #33 複数品目レシート分類はユーザー判断で正式ロードマップから削除済みです。このテストには含めません。
