# PHASE2_TEST.md

Phase 2（月次・予算判断）と、Phase 3より先に確認する毎月1日の予算設定案内の実機確認手順です。

一度に全部確認せず、2A → 2B → 2C → 2.8 の順で進めます。

---

# Phase 2A — 日々の家計判断

対象:
- #15 1日あたり使える額
- #16 使いすぎペース
- #17 異常支出

追加DBは不要です。既存の家計簿DB・月別管理DBを使います。

## 2A-0 事前確認

LINEで:

```text
今月
```

全体予算が未設定なら:

```text
予算設定
```

で今月の全体予算を設定します。

## 2A-1 今日使える

```text
今日使える
```

確認:
- 残り予算
- 残り日数
- 1日あたり使える額
- 現在の1日平均

## 2A-2 支出ペース

```text
ペース
```

確認:
- 月の経過率
- 予算消化率
- 現在支出
- 現在ペースの月末予測
- 予算内/やや速い/かなり速い の判定

## 2A-3 異常支出

```text
異常支出
```

候補がなければ「見つかりませんでした」、候補があれば日付・店名・金額・基準比が表示されること。

---

# Phase 2B — 月次判断

対象:
- #11 月締め
- #12 月次AIレビュー
- #13 予算提案

## 2B-0 月別管理DB

| 名前 | 型 |
|---|---|
| `締め済み` | Checkbox |
| `締め日時` | Date |
| `確定支出` | Number |

## 2B-1 予算提案

```text
予算提案
```

確認:
- 過去実績から全体予算・ジャンル別予算の目安
- 自動でNotionの予算を書き換えない

## 2B-2 月締め

```text
月締め
月締め 2026-08
```

確認:
- 支出合計
- 件数
- 予算差額
- 上位ジャンル

## 2B-3 月締め確定

```text
月締め確定 2026-08
月締め確定強制 2026-08
```

通常確定はカード未処理があると停止すること。

## 2B-4 月次AIレビュー

```text
AI Model
月次レビュー 2026-08
```

対象月の集計に基づくレビューがPushされること。

---

# Phase 2C — 将来予測・目標

対象:
- #20 年間予測
- #21 貯金目標

## 2C-1 年間予測

```text
年間予測
年間予測 2025
```

## 2C-2 貯金目標

```text
貯金目標追加 旅行 300000 50000 2027-03-31
貯金目標
貯金更新 旅行 80000
```

---

# Phase 2.8 — 毎月1日の予算設定案内

状態: **実装済み・要実機確認**

Phase 3より先に確認します。

## 2.8-0 Apps Scriptへ反映

GitHub最新版の次のファイルをApps Scriptへ丸ごと反映します。

```text
gas/FinanceReports.gs
```

Script Propertiesに次があることを確認します。

```text
LINE_USER_ID
LINE_CHANNEL_ACCESS_TOKEN
```

秘密値はコードへ直書きしません。

## 2.8-1 通知だけテスト

Apps Scriptで:

```text
testMonthlyBudgetNotice
```

を実行します。

確認:
- エラーなく完了する
- LINEへ「📅 毎月の予算設定」が届く
- `設定する` / `後でする` の2ボタンが表示される

## 2.8-2 設定する

LINEで `設定する` を押します。

期待:

```text
📅 今月の全体予算を入力してください。例: 100000
やめる場合は キャンセル
```

のような案内が返ること。

## 2.8-3 予算保存

テスト用の金額を数字だけ送ります。

例:

```text
100000
```

確認:
- LINEに保存成功メッセージが返る
- Notion月別管理DBの当月ページが存在する
- 当月 `全体予算` が入力値へ更新される

実際に使う予算と異なるテスト値を入れた場合は、テスト後に正しい値へ戻してください。

## 2.8-4 後でする

再度 `testMonthlyBudgetNotice` を実行し、今度は `後でする` を押します。

確認:
- 予算入力待ち状態へ入らない
- その後通常のLINEコマンドが使える

## 2.8-5 毎月1日トリガー作成

上記がすべて正常ならApps Scriptで:

```text
installMonthlyBudgetNoticeTrigger
```

を1回実行します。

Apps Scriptのトリガー画面で:

```text
関数: sendMonthlyBudgetNotice
毎月1日
午前6時〜7時
```

になっていることを確認します。

`installMonthlyBudgetNoticeTrigger` は同じ関数の既存トリガーを削除してから1つ作り直すため、重複登録を防ぎます。

## 2.8-6 完了条件

```text
LINE通知 OK
設定する → 金額入力 OK
Notion全体予算更新 OK
後でする OK
毎月1日6時台トリガー OK
```

ここまで確認できたらPhase 2.8を完了にし、その後Phase 3Aへ進みます。

---

# コマンド一覧

```text
家計判断
今日使える
ペース
異常支出
予算設定
予算一覧
予算提案
月締め
月締め YYYY-MM
月締め確定 YYYY-MM
月締め確定強制 YYYY-MM
月次レビュー
月次レビュー YYYY-MM
年間予測
年間予測 YYYY
貯金目標
貯金目標追加 目標名 目標額 [現在額] [期限]
貯金更新 目標名 現在額
```
