# PHASE2_TEST.md

Phase 2（月次・予算判断）と、Phase 3より先に確認する追加機能の実機確認手順です。

一度に全部確認せず、2A → 2B → 2C → 2.8 → 2.9 → 2.10 の順で進めます。

---

# Phase 2A — 日々の家計判断

対象:
- #15 1日あたり使える額
- #16 使いすぎペース
- #17 異常支出

追加DBは不要です。既存の家計簿DB・月別管理DBを使います。

## 2A-0 事前確認

LINEで:

```text
今月
```

全体予算が未設定なら:

```text
予算設定
```

で今月の全体予算を設定します。

## 2A-1 今日使える

```text
今日使える
```

確認:
- 残り予算
- 残り日数
- 1日あたり使える額
- 現在の1日平均

## 2A-2 支出ペース

```text
ペース
```

確認:
- 月の経過率
- 予算消化率
- 現在支出
- 現在ペースの月末予測
- 予算内/やや速い/かなり速い の判定

## 2A-3 異常支出

```text
異常支出
```

候補がなければ「見つかりませんでした」、候補があれば日付・店名・金額・基準比が表示されること。

---

# Phase 2B — 月次判断

対象:
- #11 月締め
- #12 月次AIレビュー
- #13 予算提案

## 2B-0 月別管理DB

| 名前 | 型 |
|---|---|
| `締め済み` | Checkbox |
| `締め日時` | Date |
| `確定支出` | Number |

## 2B-1 予算提案

```text
予算提案
```

確認:
- 過去実績から全体予算・ジャンル別予算の目安
- 自動でNotionの予算を書き換えない

## 2B-2 月締め

```text
月締め
月締め 2026-08
```

確認:
- 支出合計
- 件数
- 予算差額
- 上位ジャンル

## 2B-3 月締め確定

```text
月締め確定 2026-08
月締め確定強制 2026-08
```

通常確定はカード未処理があると停止すること。

## 2B-4 月次AIレビュー

```text
AI Model
月次レビュー 2026-08
```

対象月の集計に基づくレビューがPushされること。

---

# Phase 2C — 将来予測・目標

対象:
- #20 年間予測
- #21 貯金目標

## 2C-1 年間予測

```text
年間予測
年間予測 2025
```

## 2C-2 貯金目標

```text
貯金目標追加 旅行 300000 50000 2027-03-31
貯金目標
貯金更新 旅行 80000
```

---

# Phase 2.8 — 毎月1日の予算設定案内

状態: **実装済み・要実機確認**

GitHub最新版の `gas/FinanceReports.gs` をApps Scriptへ丸ごと反映します。

Script Properties:

```text
LINE_USER_ID
LINE_CHANNEL_ACCESS_TOKEN
```

通知テスト:

```text
testMonthlyBudgetNotice
```

確認:
- LINEへ「📅 毎月の予算設定」が届く
- `設定する` / `後でする` が表示される
- `設定する` → 金額入力 → Notion月別管理DBの当月 `全体予算` が更新される
- `後でする` → 入力待ちにならない

正常なら:

```text
installMonthlyBudgetNoticeTrigger
```

を1回実行し、`sendMonthlyBudgetNotice` が毎月1日6時台のトリガーになっていることを確認します。

---

# Phase 2.9 — 貸し借り管理

状態: **実装済み・要Render設定/実機確認**

Render環境変数:

```text
NOTION_LOAN_DATABASE_ID=f9b2c4eb59ea4c13b968f8d9b48663bc
```

設定後にRenderを再デプロイします。

## 2.9-1 登録

LINE:

```text
貸した テスト 100 テスト用
借りた テスト2 200 テスト用
```

確認:
- `貸し借り管理` DBへ2件作成される
- 種類が貸した/借りたで分かれる
- 状態が `未精算`
- 日付が当日
- 家計簿DBには自動追加されない

## 2.9-2 一覧

```text
貸し借り一覧
```

確認:
- 貸している合計
- 借りている合計
- 未精算レコード一覧

が表示される。

## 2.9-3 精算

```text
精算 テスト 100
```

確認:
- `テスト / 100円` が `精算済み`
- `精算日` が入る
- 再度 `貸し借り一覧` で未精算一覧から消える

同じ相手・同じ金額の未精算レコードを2件作った場合、`精算 相手 金額` が自動更新せず停止することも安全確認します。

---

# Phase 2.10 — 「この機能ある？」+ 機能追加要望

状態: **実装済み・要Render設定/実機確認**

Render環境変数:

```text
NOTION_FEATURE_REQUEST_DATABASE_ID=76e4fe5d248e4fd1a48f45e9bdd59e8c
```

設定後にRenderを再デプロイします。

## 2.10-1 実装済み機能

```text
機能確認 貸し借り
```

期待:
- `あります` と返る
- `貸した / 借りた / 貸し借り一覧 / 精算` の使い方が出る
- 機能追加要望DBへ登録されない

## 2.10-2 ロードマップ済み機能

```text
機能確認 レシート入力
```

期待:
- まだ使えないと返る
- Phase 3A予定とわかる
- 重複要望としてDBへ新規登録しない

## 2.10-3 未知の機能

テスト専用の架空機能名を使います。

```text
機能確認 月面家計簿
```

期待:
- 現在のBotにはないと返る
- `機能追加要望` DBへ1件追加
- `状態=未確認`
- `回数=1`

同じ文言をもう一度送ります。

```text
機能確認 月面家計簿
```

確認:
- 新規行が増えない
- 同じ行の `回数=2` になる

テスト後、この架空要望はNotion上で削除または見送りにして構いません。

---

# Phase 2.8〜2.10 完了条件

```text
毎月1日予算通知 OK
貸し借り登録/一覧/精算 OK
機能確認（実装済み） OK
機能確認（ロードマップ済み） OK
未知機能 → 要望DB登録 OK
同じ要望 → 回数加算 OK
```

ここまで確認したらPhase 3Aへ進みます。

---

# コマンド一覧

```text
家計判断
今日使える
ペース
異常支出
予算設定
予算一覧
予算提案
月締め
月締め YYYY-MM
月締め確定 YYYY-MM
月締め確定強制 YYYY-MM
月次レビュー
月次レビュー YYYY-MM
年間予測
年間予測 YYYY
貯金目標
貯金目標追加 目標名 目標額 [現在額] [期限]
貯金更新 目標名 現在額
貸した 相手 金額 [メモ]
借りた 相手 金額 [メモ]
貸し借り一覧
精算 相手 金額
機能確認 機能名
```
