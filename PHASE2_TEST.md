# PHASE2_TEST.md

Phase 2（月次・予算判断）の実機確認手順です。

一度に全部確認せず、2A → 2B → 2C の順で進めます。

---

# Phase 2A — 日々の家計判断

対象:
- #15 1日あたり使える額
- #16 使いすぎペース
- #17 異常支出

追加DBは不要です。既存の家計簿DB・月別管理DBを使います。

## 2A-0 事前確認

LINEで:

```text
今月
```

今月の家計簿ダッシュボードが表示されることを確認します。

全体予算が未設定なら:

```text
予算設定
```

で今月の全体予算を設定します。

## 2A-1 今日使える

```text
今日使える
```

確認:
- 残り予算が表示される
- 残り日数が表示される
- 1日あたり使える額が表示される
- 現在の1日平均が表示される

計算は概ね:

```text
(全体予算 - 今月の支出) ÷ 月末までの残り日数
```

です。

## 2A-2 支出ペース

```text
ペース
```

確認:
- 月の経過率
- 予算消化率
- 現在支出
- 現在ペースの月末予測
- 予算内/やや速い/かなり速い の判定

が表示されること。

## 2A-3 異常支出

```text
異常支出
```

確認:
- 候補がなければ「見つかりませんでした」
- 候補があれば日付・店名・金額・基準比が表示される

異常支出は「誤登録」と断定する機能ではありません。予定していた高額支出も候補に出ます。

2Aがすべて正常なら Phase 2A 完了です。

---

# Phase 2B — 月次判断

対象:
- #11 月締め
- #12 月次AIレビュー
- #13 予算提案

## 2B-0 月別管理DBの追加項目

月別管理DBへ次の3項目を追加します。

| 名前 | 型 |
|---|---|
| `締め済み` | Checkbox |
| `締め日時` | Date |
| `確定支出` | Number |

Notion Integrationが月別管理DBへ接続されていることも確認します。

## 2B-1 予算提案

```text
予算提案
```

確認:
- 過去3か月にデータがあれば全体予算の目安が表示される
- ジャンル別の目安が表示される
- 自動でNotionの予算を書き換えない

データ不足なら「まだ提案できません」と表示されれば正常です。

## 2B-2 月締めプレビュー

引数なし:

```text
月締め
```

→ 前月を表示します。

任意月:

```text
月締め 2026-08
```

確認:
- 支出合計
- 件数
- 予算差額（予算設定済みの場合）
- 上位ジャンル
- 確定用コマンド

が表示されること。

この段階ではNotionの`締め済み`は変更されません。

## 2B-3 月締め安全ガード

今月を指定:

```text
月締め 2026-09
```

実行時点の現在月なら「今月や未来月は締められません」と表示されること。

カード未処理を1件残した状態で:

```text
月締め確定 2026-08
```

を送ると、未処理件数を表示して月締めを停止すること。

内容を確認済みで意図的に締めるテストだけ:

```text
月締め確定強制 2026-08
```

を使います。

## 2B-4 月締め確定

カード未処理が0件の状態で:

```text
月締め確定 2026-08
```

確認:
- 成功メッセージが返る
- 月別管理DBの該当月で`締め済み=true`
- `締め日時`が入る
- `確定支出`が家計簿集計と一致する

## 2B-5 月次AIレビュー

現在選択モデル確認:

```text
AI Model
```

レビュー:

```text
月次レビュー 2026-08
```

確認:
- 最初に生成開始メッセージが返る
- 後からLINE Pushでレビューが届く
- 表示上のLite/Flashが`AI Model`と一致する
- 対象月の支出集計に基づいた内容になっている

レビューは家計簿集計だけを根拠にし、外部情報を勝手に混ぜない設計です。

2Bがすべて正常なら Phase 2B 完了です。

---

# Phase 2C — 将来予測・目標

対象:
- #20 年間予測
- #21 貯金目標

## 2C-1 年間予測

```text
年間予測
```

確認:
- 今年の現在までの支出
- 現在ペースの年間予測
- 経過日数

が表示されること。

過去年:

```text
年間予測 2025
```

過去年は年間実績として表示されます。

## 2C-2 貯金目標DB

新規Notion DB:

| 名前 | 型 |
|---|---|
| `目標名` | Title |
| `目標額` | Number |
| `現在額` | Number |
| `期限` | Date |
| `有効` | Checkbox |

Integrationを接続し、Renderへ:

```text
NOTION_SAVINGS_GOALS_DATABASE_ID
```

を設定して再デプロイします。

## 2C-3 貯金目標追加

```text
貯金目標追加 旅行 300000 50000 2027-03-31
```

確認:
- Notionに目標が1件作られる
- 目標額 300000
- 現在額 50000
- 期限 2027-03-31
- 有効 true

## 2C-4 貯金目標一覧

```text
貯金目標
```

確認:
- 現在額 / 目標額
- 達成率
- 期限
- 月あたり必要額の目安

が表示されること。

## 2C-5 現在額更新

```text
貯金更新 旅行 80000
```

確認:
- Notionの現在額が80000へ更新される
- 再度`貯金目標`を送ると達成率・月あたり目安が更新される

2Cがすべて正常なら Phase 2C 完了です。

---

# Phase 2全体完了条件

```text
2A 日々の家計判断 OK
2B 月次判断 OK
2C 将来予測・目標 OK
```

すべて確認できたらPhase 2を「完了」に変更できます。

Phase 3へ進む前に、不具合があればPhase 2内で修正してください。

---

# コマンド一覧

```text
家計判断
今日使える
ペース
異常支出
予算提案
月締め
月締め YYYY-MM
月締め確定 YYYY-MM
月締め確定強制 YYYY-MM
月次レビュー
月次レビュー YYYY-MM
年間予測
年間予測 YYYY
貯金目標
貯金目標追加 目標名 目標額 [現在額] [期限]
貯金更新 目標名 現在額
```
