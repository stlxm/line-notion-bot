# LINE Notion Bot

LINEを入口に、家計簿・予算・カード・固定費・貸し借り・メモ・買い物・特売・URL保存・Notion・Gemini AIをまとめて扱う個人向けBotです。

## ドキュメント

- `SETUP.md`: 初期構築、環境変数、Notion DB、GAS、Render設定
- `MAINTENANCE.md`: 日常保守、障害切り分け、復旧
- `DEVELOPMENT.md`: 長期ロードマップ、進捗、次回再開位置
- `UI_DESIGN.md`: LINE UIとPostback設計ルール
- `PHASE1_TEST.md`: カード自動化テスト
- `PHASE2_TEST.md`: 月次・予算判断テスト
- `PHASE3_TEST.md`: Phase 3A/3B テスト
- `PHASE5_TEST.md`: Phase 4/5 実機テスト
- `FLYER_TEST.md`: サミットチラシ・生活カレンダー実機テスト
- `gas/README.md`: GAS固有設定

機能変更時は README.md / SETUP.md / DEVELOPMENT.md を更新し、UI変更時は UI_DESIGN.md も更新します。

---

# メニュー

`メニュー` はフェーズ名を見せず、**利用できる機能を機能単位で全部出す**方針です。

横スワイプできるカテゴリ:

```text
✨ 案内・入口
🧾 家計簿・入力
📊 予算・分析
🎯 月次・固定費・貯金
✏️ 修正・取り消し
💳 カード
💸 貸し借り
📝 メモ・買い物
🛒 特売・あとで見る
🤖 AI
🗂 Notion・データ
```

`Phase4` / `Phase5` のような開発上の名前はメニューに出しません。

---

# 未認識コマンド

コマンドが見つからないとき、毎回メニュー選択画面は自動表示しません。

```text
そのコマンドはありません。メニューから機能を選んでください。
「ヘルプ」か「メニュー」と送ってください。
```

必要なときだけユーザーが `ヘルプ` または `メニュー` を呼び出します。

---

# 全コマンド一覧

`コマンド` と送ると、実装済みの主要コマンドをカテゴリ別にまとめて表示します。

含むもの:

```text
案内 / 家計簿入力 / 予算 / 分析 / 月次 / 固定費 / 貯金目標
修正 / 貸し借り / カード / メモ / 買い物 / 特売 / URL
AI / Notion
```

---

# カード店名の小書き文字補正学習

状態: **実装済み・要実機確認**

カード会社の明細で、小書き文字が大きい文字として届くケースを店名変更から学習します。

例:

```text
サミツト
↓ 店名変更
サミット
```

この変更が「大きい仮名 → 対応する小書き仮名」だけだった場合は、今後の同じカード店名を自動で補正します。

```text
次回: サミツト → 自動で サミット
```

さらに、修正時点で同じ旧店名が残っていれば次もまとめて更新します。

```text
カード未処理DB
過去の家計簿DB
固定費DB
```

店名の追加・削除・別文字への変更まで含む場合は誤学習を避けるため自動学習せず、現在の1件だけ変更します。

---

# Phase 4 — メモ・URL

状態: **実装済み・要実機確認**

```text
#50 メモ期限
#53 メモ自動分類
#54 買い物リスト
#56 URL保存時タイトル取得
#57 URLカテゴリ分類
```

例:

```text
メモ 住民票を明日までに提出
メモ レポートを金曜までに出す
メモ 旅行準備を今週中に終わらせる
買い物 牛乳
買い物リスト
買った 牛乳
https://example.com/
```

期限は、今日 / 明日 / 明後日 / N日後 / 曜日 / 来週月曜 / 今週中 / 今月末 / 9月20日 / YYYY-MM-DD などを文章から読み取ります。期限表現がなければ勝手に推測しません。

---

# Phase 5 — AIの説明責任と改善

状態: **主要機能実装済み・要実機確認**

```text
#61 AI回答 👍 / 👎 評価
#67 AIが参照したDBを表示
#68 AI回答の根拠表示
#72 AI改善内容をDBルーターへ反映
#73 Notion DBヘルスチェック
```

AI回答後に `👍 良い / 👎 改善したい` の評価ボタンを表示します。評価はAI改善ログDBへ保存されます。

```text
AI 今月の食費を分析して
AI評価 👍
AI評価 👎
AI改善
DBヘルスチェック
```

---

# 今日の特売

状態: **LINEコマンド実機確認済み / 自動反映モード実装済み・要GAS切替確認**

```text
特売
特売情報
今日の特売
本日の特売
サミット特売
```

取得元:

```text
NOTION_FLYER_DATABASE_ID=684f959e451047389505a95ed368a7d6
```

旧 `3d90efb323d080b5999bed1820a6665e` は削除済みDBなので使用しません。

## チラシの手動確認を廃止

Notionで `確認待ち → 確認済み` に変更する作業は不要です。

新しい `gas/FlyerAutoMode.gs` が、Gemini解析に成功したチラシを自動で生活カレンダーへ有効化します。旧仕様で `確認待ち` のまま残っている掲載中データも自動で `確認済み / 有効=true` にします。

LINEの `特売情報` コマンド側も `確認状態` や `有効` の手動操作には依存せず、店舗・種類・掲載日が今日に一致する特売を表示します。

Apps Scriptへ `gas/FlyerAutoMode.gs` を追加後、1回だけ次を実行します。

```text
installDailySummitLifeCalendarAutoTrigger
```

以降の日次入口:

```text
runDailySummitLifeCalendarAutoAutomation
```

新しいチラシを取得・解析できた場合は、そのままNotionとLINE特売へ反映されます。価格・在庫については店頭表示を優先してください。

---

# Phase 3A / 3B

状態: **実装済み・要実機確認**

```text
今日サミットで2380円使った
支出テンプレート
本日のレポート
予算 お菓子 3000
直前登録
直前修正 金額 1500
直前取り消し
```

#32 / #33 のレシート系は正式ロードマップから削除済みです。

---

# 貸し借り

```text
貸した
貸した 田中 3000 ランチ代
借りた
借りた 田中 2000
貸し借り一覧
精算
精算 田中 3000
```

単独の `貸した` / `借りた` / `精算` は入力形式を案内します。

---

# AIモデル

```text
AI Lite   → gemini-3.5-flash-lite
AI Flash  → gemini-3.6-flash
AI Model  → 現在モデル確認
```

---

# 開発ロードマップ

現在の優先順:

```text
Phase 4 — 主要実装済み・要実機確認
Phase 5 — 主要実装済み・要実機確認
Phase 3C — 次
Phase 6
Phase 7
```

Phase 3C:

```text
#39 家計簿検索
#40 条件付き検索
#45 店別ランキング
#46 小額支出積み上げ分析
```

詳細は `DEVELOPMENT.md` を正本とします。
