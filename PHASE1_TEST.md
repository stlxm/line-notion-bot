# Phase 1 カード自動化 実機テスト

Phase 1導入後に、LINE / Render / Notion / GASを順番に確認するためのチェックリストです。

## 事前準備

- Renderがmain最新コミットでデプロイ成功している。
- `カード学習ルール` DBを作成済み。
- Notion Integrationをカード学習ルールDBへ接続済み。
- Renderに`NOTION_CARD_RULES_DATABASE_ID`を設定済み。
- `CARD_AUTO_REGISTER_MIN_MATCHES`は通常`3`。
- 最新`gas/FinanceReports.gs`をApps Scriptへコピー済み。
- `sendMonthEndCardCheck`を1日1回のトリガーへ追加済み。

## 1. 通常分類と学習

1. LINEで`カード未処理`。
2. 1件ジャンルを選ぶ。
3. 家計簿へ保存される。
4. カード未処理DBから該当ページがアーカイブされる。
5. カード学習ルールDBに店が作成される。
6. `学習回数=1`、`一致回数=1`、`自動登録=false`を確認。

同じ店をもう一度分類し、次回画面で`おすすめ: <ジャンル>`または`★<ジャンル>`が出ることを確認します。

## 2. ジャンル変更時の安全確認

同じ店を前回と違うジャンルに分類します。

期待:
- ジャンルが新しい値へ更新。
- 一致回数が1へ戻る。
- 自動登録はOFFへ戻る。

## 3. 同じ店まとめ処理

同じカード＋同じ店の未処理が2件以上ある状態で`カード未処理`。

期待:
- `同じ店 N件をまとめる`ボタンが出る。
- ボタン→ジャンル選択で複数件を処理。
- 保存成功分だけ未処理から消える。
- 保存失敗・重複候補は未処理に残る。

## 4. 家計簿重複ガード

既に家計簿にあるものと同じ日付・金額・カード・店名の未処理を分類します。

期待:
- 即保存せず`重複候補があります`。
- `それでも保存する`で明示的に保存可能。
- `重複として処理済みにする`で家計簿へ追加せず未処理だけ消せる。

注意: 同日・同額・同店でも正当な複数利用はあり得るため、重複候補は内容を確認してください。

## 5. 自動登録ON

同じ店を同じジャンルへ3回以上分類し、一致率100%にします。

LINEで:

```text
カード自動登録
```

期待:
- 対象店に`自動登録ON`ボタンが出る。
- ON後、カード学習ルールDBの`自動登録=true`。

次回同じ店のカードメールを検知すると:
- 家計簿へ自動保存。
- 未処理キューに残らない。
- LINEへ自動登録通知。

家計簿に重複候補がある場合は自動登録せず、未処理として確認へ回します。

## 6. 自動登録OFF

`カード自動登録`からONルールの`自動登録OFF`を押します。

期待:
- DBの`自動登録=false`。
- 次回以降は通常の未処理フローへ戻る。

## 7. サブスク

カード未処理画面:
- `固定費`は表示されない。
- `サブスク`は表示される。

サブスクを選択:
- 固定費DBへ作成または更新。
- 今回分は家計簿へ保存。
- 次回同じカード＋同じ店は未処理/LINE通知から除外。

解除は固定費DBの`有効`をOFF。

## 8. Postback 300文字対策

長い日本語店名のカードでジャンルボタンを押します。

期待:
- Render `/callback` が200。
- `PostbackAction data at most 300 characters`が出ない。

## 9. 月末チェック

通常はGASの`sendMonthEndCardCheck`を毎日1回実行します。月末以外はLINE通知なし。

テスト時はRender endpointへ`force=true`を送れる環境がある場合のみ強制確認可能です。本番GAS関数は通常モードのまま運用してください。

月末期待:
- 未処理0件 → 完了通知。
- 未処理あり → 残件数警告。

## 10. ログ確認

Renderでエラーがある場合は最初のTracebackから確認します。Notion関連では次の語を検索します。

```text
カード学習ルール
家計簿重複検索
カード未処理DB
固定費マスタ
固定費除外ルール
```

GASでは:

```text
[解析成功]
[Render] /api/card-pending: 200
LINEレスポンス: 200
```

を確認します。

## Phase 1完了判定

上記1〜8が正常で、月末チェックのendpoint/GAS設定まで完了していればPhase 1の実環境導入完了です。
