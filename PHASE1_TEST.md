# Phase 1 カード自動化 実機テスト

Phase 1導入後に、LINE / Render / Notion / GASを順番に確認するためのチェックリストです。

## 事前準備

- Renderがmain最新コミットでデプロイ成功している。
- `カード学習ルール` DBを作成済み。
- Notion Integrationをカード学習ルールDBへ接続済み。
- Renderに`NOTION_CARD_RULES_DATABASE_ID`を設定済み。
- `CARD_AUTO_REGISTER_MIN_MATCHES`は通常`3`。
- 最新`gas/FinanceReports.gs`をApps Scriptへコピー済み。
- `sendMonthEndCardCheck`を1日1回のトリガーへ追加済み。

## 0. 未処理カードがない場合

本物の未処理カードが0件でもテストできます。LINEで:

```text
カードテスト
```

と送信してください。

期待:
- `Phase1テストショップ`のテスト用未処理カードが1件作成される。
- 通常のカードジャンル選択画面が表示される。
- Gmailやカード会社メールには影響しない。

`カードテスト`は1回送るたびに1件追加できます。同じテストショップで複数件作れるため、一括分類や学習回数のテストにも利用できます。

注意:
- ジャンルを保存すると家計簿DBへテスト支出が保存されます。
- カード学習ルールDBにもテストショップが保存されます。
- 不要なテストデータはNotion側で削除/アーカイブしてください。
- 家計簿へ保存したくないテストは`登録しない`を選びます。

## 1. 通常分類と学習

本物の未処理、または`カードテスト`で作成した1件を使います。

1. LINEでカードのジャンルを1つ選ぶ。
2. 家計簿へ保存される。
3. カード未処理DBから該当ページがアーカイブされる。
4. カード学習ルールDBに店が作成される。
5. `学習回数=1`、`一致回数=1`、`自動登録=false`を確認。

もう一度`カードテスト`を送り、同じジャンルを選びます。

期待:
- 画面に`おすすめ: <ジャンル>`または`★<ジャンル>`が出る。
- 学習回数と一致回数が増える。

## 2. ジャンル変更時の安全確認

`カードテスト`をもう一度作成し、前回と違うジャンルに分類します。

期待:
- ジャンルが新しい値へ更新。
- 一致回数が1へ戻る。
- 自動登録はOFFへ戻る。

## 3. 同じ店まとめ処理

LINEで`カードテスト`を2〜3回送り、未処理のまま複数件作ります。

その後`カード未処理`。

期待:
- `同じ店 N件をまとめる`ボタンが出る。
- ボタン→ジャンル選択で複数件を処理。
- 保存成功分だけ未処理から消える。
- 保存失敗・重複候補は未処理に残る。

## 4. 家計簿重複ガード

一度保存したテスト支出と同じ日付・金額・カード・店名になる未処理がある場合に確認します。本物データで偶然一致した場合でも構いません。

期待:
- 即保存せず`重複候補があります`。
- `それでも保存する`で明示的に保存可能。
- `重複として処理済みにする`で家計簿へ追加せず未処理だけ消せる。

注意: 同日・同額・同店でも正当な複数利用はあり得るため、重複候補は内容を確認してください。

## 5. 自動登録ON

同じ店を同じジャンルへ3回以上分類し、一致率100%にします。テストする場合は`カードテスト`を3回以上使えます。

LINEで:

```text
カード自動登録
```

期待:
- 対象店に`自動登録ON`ボタンが出る。
- ON後、カード学習ルールDBの`自動登録=true`。

本当の新着カードメールで同じ店を検知すると:
- 家計簿へ自動保存。
- 未処理キューに残らない。
- LINEへ自動登録通知。

注意: `カードテスト`コマンドはテスト用未処理を直接作るため、GAS→`/api/card-pending`の新着自動登録経路そのものは通りません。自動登録の最終確認だけは実カードメール、またはAPIテスト環境で行います。

家計簿に重複候補がある場合は自動登録せず、未処理として確認へ回します。

## 6. 自動登録OFF

`カード自動登録`からONルールの`自動登録OFF`を押します。

期待:
- DBの`自動登録=false`。
- 次回以降は通常の未処理フローへ戻る。

## 7. サブスク

カード未処理画面:
- `固定費`は表示されない。
- `サブスク`は表示される。

サブスクを選択:
- 固定費DBへ作成または更新。
- 今回分は家計簿へ保存。
- 次回同じカード＋同じ店は未処理/LINE通知から除外。

テストショップをサブスクにした場合、その後の`カードテスト`も固定費除外に引っかかるため、テスト継続時は固定費DBの`Phase1テストショップ`で`有効`をOFFにしてください。

## 8. Postback 300文字対策

長い日本語店名の本物カードがある場合に確認します。

期待:
- Render `/callback` が200。
- `PostbackAction data at most 300 characters`が出ない。

## 9. 月末チェック

通常はGASの`sendMonthEndCardCheck`を毎日1回実行します。月末以外はLINE通知なし。

テスト時はRender endpointへ`force=true`を送れる環境がある場合のみ強制確認可能です。本番GAS関数は通常モードのまま運用してください。

月末期待:
- 未処理0件 → 完了通知。
- 未処理あり → 残件数警告。

## 10. ログ確認

Renderでエラーがある場合は最初のTracebackから確認します。Notion関連では次の語を検索します。

```text
カード学習ルール
家計簿重複検索
カード未処理DB
固定費マスタ
固定費除外ルール
```

GASでは:

```text
[解析成功]
[Render] /api/card-pending: 200
LINEレスポンス: 200
```

を確認します。

## Phase 1完了判定

テスト用カードで1〜7を確認し、実カード/GASで新着自動登録経路と月末チェック設定まで確認できればPhase 1の実環境導入完了です。
