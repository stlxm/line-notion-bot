# Phase 4 / 5 実機テスト

GitHub最新版をRenderへ再デプロイしてから実施します。

## Phase 4

### 1. メニュー

```text
メニュー
```

確認:
- `🛒 特売・買い物` に買い物リストがある
- `📝 メモ・あとで見る` にPhase 4入口がある

### 2. 期限付きメモ

```text
メモ 住民票を明日までに提出
```

確認:
- `分類: やること`
- 明日の日付が期限として返る
- NotionメモDBの `期限 / 分類 / 完了` が正しい

### 3. 買い物リスト

```text
買い物 Phase4テスト牛乳
買い物リスト
買った Phase4テスト牛乳
買い物リスト
```

確認:
- 追加時は `分類=買い物`
- 1回目の一覧に表示
- `買った` 後に `完了=true`
- 2回目の一覧から消える

### 4. URL

```text
https://example.com/
```

確認:
- URL自体が保存される
- `ページタイトル / カテゴリ / ドメイン / 保存日時` が入る
- タイトル取得失敗時も保存処理自体は失敗しない

## Phase 5

### 5. メニュー

```text
メニュー
```

確認:
- `🤖 AI検索・改善` に `👍 直前AI回答を評価`
- `👎 直前AI回答を評価`
- `Phase 5の使い方`
- `DBヘルスチェック`

評価ボタンはMessage actionなので、押した瞬間にコマンドがトークへ表示されること。

### 6. DBヘルス

```text
DBヘルスチェック
```

確認:
- 各DBが ✅ / ⚠️ / ❌ で表示される
- APIキーや秘密値そのものが表示されない
- メモDBの `期限 / 分類 / 完了` を認識する
- URL DBの `ページタイトル / カテゴリ / ドメイン / 保存日時` を認識する
- AI改善ログの `評価 / 参照DB / 根拠` を認識する

### 7. AI参照DB・根拠

```text
AI 今月の食費を分析して
```

確認:
- 最初に処理中メッセージが返る
- 完了後の回答に `【参照DB】` がある
- `【根拠】` がある
- 末尾に `AI評価 👍 / AI評価 👎` の案内がある

### 8. 👍 評価

```text
AI評価 👍
```

確認:
- 成功メッセージ
- AI改善ログDBに新規行
- `評価=👍`
- `参照DB / 根拠` が保存される

### 9. 👎 評価 + 改善

新しいAI質問を1回実行後:

```text
AI評価 👎
AI改善
```

確認:
- `評価=👎` が保存される
- `AI改善` の既存フローへ進める
- 期待する回答を入力して `評価=改善` のログが保存される

### 10. 機能確認

```text
機能確認 買い物リスト
機能確認 URL分類
機能確認 AI評価
機能確認 DBヘルス
```

すべて実装済みとして案内されること。

## 完了条件

すべて通過したら:

```text
Phase 4 → 実機確認済み
Phase 5 → 実機確認済み
次 → Phase 3C (#39 / #40 / #45 / #46)
```
